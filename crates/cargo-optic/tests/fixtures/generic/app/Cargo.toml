[package]
name = "optic-mvp-app"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[features]
optional-kernel = ["dep:optic-mvp-optional-kernel"]

[dependencies]
optic-mvp-kernel = { path = "../kernel" }
optic-mvp-optional-kernel = { path = "../optional-kernel", optional = true }
