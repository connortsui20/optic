//! Supplies an emitted library function for installed source and LLVM queries.
//!
//! The installation script initializes Git before capture without an Optic ignore rule.

/// Returns the value that distinguishes original and edited evidence.
#[inline(never)]
pub fn captured_value() -> u64 {
    42
}
